# AGENTS.md

## 项目定位

本项目是“刷题宝 PWA 手机版”，基于 QuarkPixel/quiz-app 改造，目标是在 iOS Safari 和安卓 Chrome/Edge 上作为手机本地刷题工具使用。

## 开发规则

1. 第一版只做个人本地使用，不做账号、后台、云同步、多人考试统计。
2. 优先保证手机竖屏体验，电脑端能正常使用即可。
3. 题库和进度默认只保存在当前浏览器本地，不上传服务器。
4. 每次只实现一个明确功能，不做无关重构。
5. 修改前说明准备改什么，修改后说明改了哪些文件、如何测试。
6. 涉及行为变更时先补测试，再改实现。
7. 保留原项目 MIT License 信息，README 中说明改造来源。
8. 题库字段以 JSON 为准，第一版重点支持 judgment、single、multiple；blank 保留原项目能力。
9. 安全环保类示例题表达应专业、准确、简洁，不编造具体制度条款。
10. 每个稳定阶段提醒保存 Git 版本。

## 题库格式约定

- `id`：题目唯一编号，不得重复，不得以 `^` 开头。
- `type`：题型，支持 `judgment`、`single`、`multiple`、`blank`。
- `category`：可选分类，例如安全生产、隐患治理、外包队伍。
- `question`：题干。
- `options`：选择题选项数组。
- `answer`：答案，选择题使用从 0 开始的索引数组。
- `explanation`：可选解析，用于答题后展示。
